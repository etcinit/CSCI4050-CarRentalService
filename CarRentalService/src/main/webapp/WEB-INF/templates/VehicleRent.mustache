<div class="row">
	<div class="col-md-12">
		<div class="page-header">
			<h1>Vehicle rental overview</h1>
		</div>
	</div>
</div>
<div class="row">
	<div class="col-md-4">
		<h4>Selected vehicle:</h4>
		<p>This is the vehicle that will be used when looking for available times:</p>
		{{{vehicle_card}}}
	</div>
	<div class="col-md-8">
		<h4>Rental times:</h4>
		<p>Enter the dates for the rental below:</p>
		<form role="form" action="{{base_url}}/rent" method="post">
			<div class="form-group">
				<label for="rentalVehicleUID">Vehicle UID:</label>
				<input type="text" class="form-control" id="rentalVehicleUID" placeholder="UID missing" value={{uid}} disabled="disabled">
			</div>
			
			<input type="hidden" name="rentalVehicleUID" value={{uid}}>
			
			<div class="form-group">
				<label for="rentalStartDate">Start date and time:</label>
				<input type="datetime-local" class="form-control" id="rentalStartDate" name="rentalStartDate" required="required">
			</div>
			
			<div class="form-group">
				<label for="rentalEndDate">End date and time:</label>
				<input type="datetime-local" class="form-control" id="rentalEndDate" name="rentalEndDate" required="required">
			</div>
			
			<button type="submit" class="btn btn-primary pull-right">Continue</button>
		</form>
		<p>By clicking continue we will try to find if this vehicle is available on these dates.</p>
	</div>
</div>